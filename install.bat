powershell -Command "$connection = Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue; if ($connection) { Stop-Process -Id $connection.OwningProcess -Force }; git pull origin main; cls; git pull origin main; npm i; Remove-Item -Recurse -Force .nuxt; npm run build; node remove.js; npm start"
